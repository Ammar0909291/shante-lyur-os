export enum InventoryCategory {
  CONSUMABLE = 'CONSUMABLE',   // used during procedures, not for resale
  RETAIL = 'RETAIL',           // sold to clients
  EQUIPMENT = 'EQUIPMENT',     // tools/devices, tracked by unit
}
